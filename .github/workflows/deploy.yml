name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 31.97.72.136
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            bash /home/ulscrm/deploy.sh
